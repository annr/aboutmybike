var assert = require('assert');
var prepPhoto = require('../../helpers/prep_photo');

describe('prepPhoto > ', function() {

  describe('run()', function() {
    describe('normalizes photo to the 4:3 ratio, cropping but only if necesssary.', function() {

      it('takes fields from upload form and created a new photo', function() {
        
      });

    });
  });

});