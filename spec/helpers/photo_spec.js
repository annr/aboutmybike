var assert = require('assert');
var photoHelper = require('../../helpers/bike');

describe('photoHelper > ', function() {

/* EXPOSED FUNCTIONS:


module.exports = {
  optimizeAndStoreCopies,
  replacePathWildcard,
  readAndStoreFile,
  getStoredPath,
  getFullStoredPath,
  getPhotoProperties,
  storeOriginal,
}

*/

  describe('optimizeAndStoreCopies()', function() {
    it('calls optimizeAndStoreCopy x times', function() {

    });
  });


  describe('optimizeAndStoreCopy()', function() {
    describe('calls im.resize with different options in different cases and created resources with the correct values', function() {

    });
  });

  describe('getFullStoredPath()', function() {
    it('returns path with /photos if prod', function() {

    });
    it('uses current date for subfolder', function() {

    });
  });

});