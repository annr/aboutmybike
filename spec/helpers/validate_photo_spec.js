var assert = require('assert');
var validatePhoto = require('../../helpers/validate_photo');

describe('validatePhoto > ', function() {

  describe('run()', function() {
    describe('detects label of photo using rekognition', function() {

      it('returns "bicycle" if bicycle is detected in photo', function() {

      });

      it('returns "not_bicycle" if bicycle is NOT detected in photo', function() {

      });

    });
  });

});